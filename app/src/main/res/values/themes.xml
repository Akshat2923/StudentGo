<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="Theme.StudentGo" parent="Theme.Material3.Light">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/maroon</item>
        <item name="colorPrimaryVariant">@color/maroon</item>
        <item name="colorOnPrimary">@color/creme</item>

        <!-- Secondary brand color -->
        <item name="colorSecondary">@color/creme</item>
        <item name="colorSecondaryVariant">@color/maroon</item>
        <item name="colorOnSecondary">@color/maroon</item>

        <!-- Status bar color -->
        <item name="android:statusBarColor">@color/design_default_color_background</item>

        <!-- Customize other colors as needed -->
    </style>
</resources>